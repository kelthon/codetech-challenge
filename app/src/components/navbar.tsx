import styled from "styled-components";

const Navbar = styled.nav`
`;

function NavBar() {
  return (
    <nav className="navbar">
      <h1 className="navbar-brand">CodeTech</h1>
    </nav>
  );
}

export default NavBar;
